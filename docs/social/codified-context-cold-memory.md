# Social Posts: Cold memory (codified context part 4)

## LinkedIn

Your CLAUDE.md handles routing. Your specialist skills carry domain expertise. But some architectural knowledge is too detailed for either.

That's cold memory: subsystem specs retrieved on demand via MCP tools when a session needs deep context.

Part 4 of my codified context series covers:

- Why specs are different from skills (and when you need both)
- How MCP servers deliver specs without bloating every session
- Contract specs for parallel development across subsystems
- The drift problem: why stale specs are worse than no specs
- A simple drift detector you can run after any session

The key insight: not all knowledge belongs in every session. Load what you need, when you need it.

https://jonesrussell.github.io/blog/codified-context-cold-memory/

#ClaudeCode #AIAssistedDev #CodifiedContext #DeveloperProductivity

## Facebook

Part 4 of my codified context series is live!

This one covers "cold memory": the architectural specs that are too detailed for your project constitution or specialist skills, but exactly what an AI session needs when it's working deep in a subsystem.

The post walks through how MCP retrieval tools deliver specs on demand, why contract specs help parallel development, and why a stale spec is actively worse than having no spec at all.

Plus a simple drift detection script you can use today.

https://jonesrussell.github.io/blog/codified-context-cold-memory/

## X (Twitter)

New post: Cold memory in AI-assisted dev.

Your CLAUDE.md routes. Skills carry expertise. But some knowledge is too detailed for either.

Specs + MCP retrieval = deep context on demand, without bloating every session.

Also: why stale specs are worse than no specs.

https://jonesrussell.github.io/blog/codified-context-cold-memory/
