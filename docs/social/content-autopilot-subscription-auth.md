## Bluesky

This blog publishes with nobody reviewing drafts. Three failure modes so far: API credit ran dry, a token was silently revoked for 4 days, and slop had no gate until a deterministic linter got added. https://jonesrussell.github.io/blog/content-autopilot-subscription-auth/ #buildinpublic

## LinkedIn

This blog's publishing pipeline runs with zero human review before a post goes live. That only works if every failure mode turns into a gate. New post walks through three real incidents: running out of pay-per-token API credit, a subscription auth token that got silently revoked for four straight days with no alert, and adding a deterministic linter that scores sentence-length variety and bans AI cliche phrases before a draft can ship. https://jonesrussell.github.io/blog/content-autopilot-subscription-auth/ #automation #githubactions #ai #devops #buildinpublic

## Facebook

New post on keeping an unsupervised AI publishing pipeline honest: switching off pay-per-token API credit, surviving a silently revoked auth token, and adding a no-API linter that catches a weak draft before it ships. https://jonesrussell.github.io/blog/content-autopilot-subscription-auth/ #ai #automation
