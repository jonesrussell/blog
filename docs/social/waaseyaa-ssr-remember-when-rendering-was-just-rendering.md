# Social copy: Remember when server-side rendering was just rendering?

**Canonical URL:** https://jonesrussell.github.io/blog/waaseyaa-ssr-remember-when-rendering-was-just-rendering/

## Facebook

Somewhere around 2016, "server-side rendering" stopped meaning "the server renders HTML." It started meaning "run your JavaScript framework on the server so it can produce the HTML that the browser will then throw away and rebuild." Waaseyaa's SSR package does the original thing: PHP, Twig templates, and HTML that arrives ready to display. No hydration required. https://jonesrussell.github.io/blog/waaseyaa-ssr-remember-when-rendering-was-just-rendering/ #php #ssr #webdev #waaseyaa

## X (Twitter)

"Server-side rendering" used to just be called "rendering." Waaseyaa does what PHP always has: request in, HTML out. No hydration, no virtual DOM. https://jonesrussell.github.io/blog/waaseyaa-ssr-remember-when-rendering-was-just-rendering/

## LinkedIn

The JavaScript ecosystem spent a decade reinventing server-side rendering and gave it a new name. Waaseyaa's SSR package takes the original approach: Twig templates, field formatters, and a theme chain loader, all on PHP 8.4+. Request in, fully rendered HTML out. New post walks through how the rendering pipeline works. https://jonesrussell.github.io/blog/waaseyaa-ssr-remember-when-rendering-was-just-rendering/
